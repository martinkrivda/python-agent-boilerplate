apiVersion: v1
kind: Service
metadata:
  name: python-agent-boilerplate
  namespace: ai-agents
  labels:
    app.kubernetes.io/name: python-agent-boilerplate
spec:
  selector:
    app.kubernetes.io/name: python-agent-boilerplate
  ports:
    - name: http
      port: 80
      targetPort: 8000
